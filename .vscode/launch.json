{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug generate-tts",
            "runtimeArgs": [
                "-r",
                "ts-node/register"
            ],
            "args": [
                "${workspaceFolder}/generateTTS.ts"
            ],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
        // You can add more configurations here
    ]
}
